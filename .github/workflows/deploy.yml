name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Sync and Deploy
        run: |
          cd /home/syrian/public_html/repo
          git fetch origin main
          git reset --hard origin/main
          chmod +x deploy.sh
          ./deploy.sh
